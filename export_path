# compatible with bash and zsh

export GOTO_source_DIR=$(dirname $(realpath ${BASH_SOURCE[0]:-$0}))

export PATH=${GOTO_source_DIR}/script:$PATH
export PYTHONPATH=${GOTO_source_DIR}/package:$PYTHONPATH

if type -t chell_path_cleanup &> /dev/null
then
	chell_path_cleanup
fi
